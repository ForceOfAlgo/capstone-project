# Core data manipulation
pandas>=1.3
numpy>=1.21

# Visualization
matplotlib>=3.4
seaborn>=0.11

# Scikit-learn for modeling, preprocessing, and metrics
scikit-learn>=1.0

# Imbalanced-learn for oversampling and undersampling
imblearn>=0.9

# XGBoost for gradient boosting models
xgboost>=1.5

# Optional but recommended: suppress warnings
# (no need to install warnings; it's part of the standard library)

